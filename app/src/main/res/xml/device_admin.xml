<?xml version="1.0" encoding="utf-8"?>
<device-admin xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-policies>
        <!-- Use policies that are appropriate for the Egyptian Agent -->
        <limit-password />
        <watch-login />
        <reset-password />
        <force-lock />
        <wipe-data />
        <expire-password />
        <encrypted-storage />
        <disable-camera />
        <!-- Additional policies as needed for the application -->
    </uses-policies>
    <support-screensizes android:normalScreens="true"
                         android:largeScreens="true"
                         android:xlargeScreens="true" />
</device-admin>